name: Sync to Gitee

on:
  push:
    branches:
      - main

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Mirror to Gitee
        uses: Yikun/hub-mirror-action@master
        with:
          src: github/minnmeichan/minnmeichan.github.io
          dst: gitee/minnmeichan/minnmeichan
          dst_key: ${{ secrets.GITEE_TOKEN }}
          force_update: true
